# Leave empty in production (same server serves API + website on Render)
# For local dev with `npm run dev` in app/, use:
VITE_API_BASE_URL=http://localhost:8080

# Shop WhatsApp for Order on WhatsApp links
VITE_SHOP_WHATSAPP=919344841180
