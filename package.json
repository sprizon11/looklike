{
  "name": "looklike-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npm --prefix app ci && npm --prefix app run build && npm --prefix backend ci",
    "start": "node backend/src/server.js"
  }
}

